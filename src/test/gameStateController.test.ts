import { expect } from 'chai';
import 'mocha';

import { GameStateController } from "../ts/gameStateController";

describe('Event Controller', () => {
    it('should change the game state', () => {
        let controller = new GameStateController();


    });
});